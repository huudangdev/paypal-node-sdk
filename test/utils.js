"use strict";

var chai = require('chai'),
    expect = chai.expect,
    should = chai.should();

var paypal = require('../');
require('./configure');

describe('Utils Test', function () {
    it('first', function (done) {
        /*
        paypal.utils.get(function (error, users) {
                expect(error).equal(null);
                expect(users).to.be.empty;
            });
           */
        done();
    });
});
